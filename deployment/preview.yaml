route:
  enabled: true
image:
  registry: image-registry.openshift-image-registry.svc:5000
  pullPolicy: Always
portal:
  config:
    issuer: https://id.dev.appuio.cloud/auth/realms/appuio-cloud-dev
    clientId: appuio-control-api
  env:
    KUBERNETES_APIS: https://api.appuio.cloud:6443/apis/
